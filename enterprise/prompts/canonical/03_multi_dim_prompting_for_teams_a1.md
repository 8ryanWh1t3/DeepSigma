# Multi-Dimensional Prompting for Teams — Workbook A1 Prompt

**Category:** Governance
**Version:** 1.0

---

> **CANONICAL LOCATION:**
> [`prompts/prompt_os/START_SESSION_A1.md`](../prompt_os/START_SESSION_A1.md)
>
> This file is a compatibility pointer.
> Use `START_SESSION_A1` as the single source of truth for the Excel cell A1 system prompt.

Canonical prompts (`01_*`, `02_*`) are reusable analytical primitives.
The workbook A1 prompt (`START_SESSION_A1`) is the operational control prompt — it reads all tabs and outputs structured triage. See the canonical file for full content.
