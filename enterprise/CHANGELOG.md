# Enterprise Changelog

## [2.1.3] — Primitive Surface

`COMPATIBLE:` CERPA adaptation loop as explicit primitive layer. ParadoxOps (12 handlers), DecisionSurface portable runtime, AuthorityOps extension (AUTH-F13–F19), constraint executor, canonical core primitives. RAL retired across 17 wiki pages. 446 new tests (1,232 total). All additive — zero breaking changes.

---

## [2.1.2] — Python 3.10 Compat & Type Safety

`COMPATIBLE:` Fix ISO-8601 `Z` suffix parsing for Python 3.10. Fix pyright type errors. Zero functional changes.

---

## [2.1.1] — OpenPQL 7-Primitive Upgrade

`COMPATIBLE:` AuthorityOps OpenPQL primitives surfaced as 6 first-class modules. 40 new tests. All additive — zero breaking changes.

---

## [2.1.0] — Decision Infrastructure Hardening

`COMPATIBLE:` v2.1.0 milestone release. 23 issues closed across 7 intermediate stability releases. All 8 KPIs >= 7.0 and SSI >= 55.
