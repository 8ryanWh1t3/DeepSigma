# Scale Benchmark Artifacts

Generated outputs for stateless API scale validation:

- `report_latest.json` — machine-readable benchmark metrics
- `report_latest.md` — human-readable benchmark summary

Generate or refresh:

```bash
bash scripts/run_scale_stack.sh
```
