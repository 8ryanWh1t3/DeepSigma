# Policy Pack Schema

Path: `policy_packs/policy_pack.schema.json`

Highlights:
- `policyPackId`, `version`, `issuedAt`, optional signature
- decisionType keyed rules:
  - DTE defaults
  - degrade ladder
  - blast-radius requirements
  - required verifiers
  - tool allow/deny lists
