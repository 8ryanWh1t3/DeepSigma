# Verifiers (Scaffold)

Verifiers make "success means verified" real.

Each verifier returns:
- `pass` / `fail` / `inconclusive`
- details (evidence of the postcondition)

Included scaffolds:
- `read_after_write.py`
- `invariant_check.py`
